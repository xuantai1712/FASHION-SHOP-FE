import {SKU} from '../SKU';
import {ProductImage} from '../productImage';

export interface SelectedImage {
  id: number;
  imageUrl : string;
  productId :number;
  colorId : number;
  thumbnail : boolean;



}
